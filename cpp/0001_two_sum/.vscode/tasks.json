{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "make clean;make -j 4",
            "args": [
                // "-g",
                // "${file}",
                // "-o",
                // "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            }
        }
    ],
    "version": "2.0.0"
}